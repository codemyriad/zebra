// This file can be used for global type declarations
// For example, if you need to extend existing types or declare global constants/interfaces

// Example:
// declare global {
//   interface Window {
//     myCustomProperty: string;
//   }
// }

// Ensure this file is included in your tsconfig.json
// "include": ["src/**/*.d.ts", ...]

export {}; // This makes the file a module, avoid polluting global scope unless intended.
